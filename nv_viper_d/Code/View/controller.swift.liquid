{% include 'header' %}

import UIKit

final class {{ prefix }}{{ module_info.name }}Controller: ViperController {

    // MARK: Public Properties

    var output: {{ prefix }}{{ module_info.name }}ViewOutput!

    // MARK: Life Cycle

    override func viewDidLoad() {
        super.viewDidLoad()
        output.viewIsReady()
    }
}

// MARK: {{ prefix }}{{ module_info.name }}ViewInput

extension {{ prefix }}{{ module_info.name }}Controller: {{ prefix }}{{ module_info.name }}ViewInput {
    func setupInitialState() { }
}