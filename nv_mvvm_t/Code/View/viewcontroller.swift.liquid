{% include 'header' %}

import UIKit
import RxSwift
import RxCocoa

class {{ prefix }}{{ module_info.name }}ViewController: UIViewController {

    // MARK: Public Properties
    var viewModel: {{ prefix }}{{ module_info.name }}ViewModel!

    // MARK: Life cycle
    override func viewDidLoad() {
        super.viewDidLoad()
        setupUI()
    }
}

// MARK: Setup UI
private extension {{ prefix }}{{ module_info.name }}ViewController {
    func setupUI() {

    }
}

// MARK: BindableType
extension {{ prefix }}{{ module_info.name }}ViewController: BindableType {
    func bindViewModel() {
        let inputs = viewModel.inputs
        let outputs = viewModel.outputs
    }
}