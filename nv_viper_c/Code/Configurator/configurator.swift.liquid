{% include 'header' %}

final class {{ prefix }}{{ module_info.name }}Configurator {

    // MARK: Private Properties
    private var viewController: {{ prefix }}{{ module_info.name }}?

    // MARK: Public Methods
    func configureModule() -> {{ prefix }}{{ module_info.name }}ModuleInput {
        let interactor = {{ prefix }}{{ module_info.name }}Interactor()
        let presenter = {{ prefix }}{{ module_info.name }}Presenter()
        let router = {{ prefix }}{{ module_info.name }}Router()

        viewController = {{ prefix }}{{ module_info.name }}ViewController()
        viewController?.output = presenter

        presenter.view = viewController
        presenter.interactor = interactor
        presenter.router = router

        interactor.output = presenter

        router.transitionHandler = viewController

        return presenter
    }
}